# auto-generated by grc.converter

id: dvbs2_physical_cc
label: Physical Layer Framer
category: '[DVB]/DVB-S2 VCM'

parameters:
-   id: dummyframes
    label: Dummy Frames
    dtype: enum
    options: [DUMMY_FRAMES_OFF, DUMMY_FRAMES_ON]
    option_labels: ['Off', 'On']
    option_attributes:
        val: [dvbs2.DUMMY_FRAMES_OFF, dvbs2.DUMMY_FRAMES_ON]

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: complex

templates:
    imports: import dvbs2
    make: dvbs2.physical_cc(${dummyframes.val})

file_format: 1
